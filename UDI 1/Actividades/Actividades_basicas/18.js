"use strict"
{
    let frase = "Hola mundo";
    let numSubstring = 4;
     function contarPalabras(frase, numSubstring){
        return console.log(frase.substring(0, numSubstring));
     }

     contarPalabras(frase, numSubstring);
}